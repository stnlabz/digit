# Configuration

Digit's approved configuration model has not yet been defined.

Configuration schema: `UNKNOWN`

Configuration must not expand Digit's mission, authority, privileges, knowledge
sources, network access, or operational boundaries.
